{{ playbook_dir | dirname }}/logs/ansible.log {
        su ansible ansible
        weekly
        missingok
        rotate 4
        size 10M
        compress
        delaycompress
        create 644 ansible ansible
}
